package com.rememberflash.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Paleta principal — tons escuros premium com acentos vibrantes
val PrimaryDark = Color(0xFF6C5CE7)        // Roxo vibrante
val PrimaryLight = Color(0xFFA29BFE)       // Roxo claro
val SecondaryDark = Color(0xFF00CEC9)      // Teal vibrante
val SecondaryLight = Color(0xFF81ECEC)     // Teal claro
val AccentOrange = Color(0xFFFDAA5E)       // Laranja quente (destaque)
val AccentPink = Color(0xFFFF6B9D)         // Rosa vibrante (alertas)

// Superfícies — modo escuro
val SurfaceDark = Color(0xFF0D1117)        // Background principal (quase preto)
val SurfaceVariantDark = Color(0xFF161B22) // Cards e containers
val SurfaceElevatedDark = Color(0xFF21262D)// Superfícies elevadas

// Superfícies — modo claro
val SurfaceLight = Color(0xFFF8F9FA)
val SurfaceVariantLight = Color(0xFFFFFFFF)
val SurfaceElevatedLight = Color(0xFFE9ECEF)

// Texto
val OnSurfaceDark = Color(0xFFF0F6FC)
val OnSurfaceVariantDark = Color(0xFF8B949E)
val OnSurfaceLight = Color(0xFF1A1A2E)
val OnSurfaceVariantLight = Color(0xFF6C757D)

// Status
val SuccessGreen = Color(0xFF2ECC71)
val ErrorRed = Color(0xFFE74C3C)
val WarningYellow = Color(0xFFF39C12)
